# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# tasks data (local only)
/tasks_data

# local redis-server dump (e.g. running tests/queue.test.ts against a
# locally-started redis-server instead of Docker/CI's redis service)
dump.rdb

# TypeScript incremental build cache (regenerated by every tsc/build run;
# shouldn't be tracked — it was previously committed by accident on both
# sides of a merge, causing spurious conflicts)
tsconfig.tsbuildinfo
